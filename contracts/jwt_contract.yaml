jwt_contract:
  algorithm: HS256
  algorithm_allowlist:
    - HS256
  signing_key_env: JWT_SECRET_KEY
  clock_skew_tolerance_seconds: 30
  required_claims:
    user_id: integer
    username: string
    iat: integer
    exp: integer
  optional_claims: []

