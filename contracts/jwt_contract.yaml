jwt_contract:
  algorithm: RS256
  algorithm_allowlist:
    - RS256
  signing_key_env: JWT_PRIVATE_KEY
  verify_key_env: JWT_PUBLIC_KEY
  clock_skew_tolerance_seconds: 30
  required_claims:
    user_id: integer
    username: string
    iat: integer
    exp: integer
  optional_claims: []

